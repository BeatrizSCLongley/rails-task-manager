<h1>My tasks</h1>

<ul>
  <% @tasks.each do |task| %>
  <li>
    <%= link_to task.title, task_path(task) %> |
    <%= link_to '🗑', task_path(task), method: :delete %>
  </li>
  <% end %>
</ul>

<!-- Add a link to the new page to add a new instance -->
<%= link_to 'Add new task', tasks_new_path %>
