<!-- Add a link here to edit the tasks -->
<%= link_to 'Edit this task', edit_path(@task) %>

<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>

<!-- Add a link to go back -->
<%= link_to 'Go back to list', tasks_path %>
